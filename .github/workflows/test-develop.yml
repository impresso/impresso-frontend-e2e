name: Run cypress tests in the dev environment

on: 
  push:
  schedule:
    - cron: "7 */3 * * *" # every 3rd hour

jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    container:
      # Cypress Docker image from https://hub.docker.com/r/cypress/included
      # with Cypress globally pre-installed
      image: cypress/included:latest
      options: --user 1001
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: 'Create .env file'
        run: |
          touch .env
          echo USER_EMAIL=${{ secrets.DEV_TEST_USER_EMAIL }} >> .env
          echo USER_PASSWORD=${{ secrets.DEV_TEST_USER_PASSWORD }} >> .env
      # Install npm dependencies, cache them correctly
      # and run all Cypress tests
      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          browser: chrome
        env:
          CYPRESS_INSTALL_BINARY: 0
      # - name: Slack Notification
      #   uses: rtCamp/action-slack-notify@v2
      #   # run only if the previous step had errors
      #   if: failure()
      #   env:
      #     SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
      #     SLACK_CHANNEL: ci-notifications
      #     SLACK_COLOR: failure
      #     SLACK_ICON: https://avatars.githubusercontent.com/u/29770928?s=200&v=4
      #     SLACK_MESSAGE: 'One ore more Cypress tests failed on the dev environment. Please check the GitHub action logs for more details.'
      #     SLACK_TITLE: 'Dev environment: Cypress tests failure'
      #     SLACK_USERNAME: GitHub E2E Workflow
      #     MSG_MINIMAL: ref,commit